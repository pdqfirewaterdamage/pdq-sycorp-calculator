{
  "name": "pdq-restoration-calc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "source .env && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.72.0"
  }
}
